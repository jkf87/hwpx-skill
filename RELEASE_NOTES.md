# v1.13.2 — 원본 제목이 딸려오던 문제 + 제목행 높이 (2026-08-21)

다른 기관 문서(`.hwp`)를 레퍼런스로 조판하다 드러난 두 가지를 고쳤습니다.

## 1. 첫 문단이 원본 제목 배너를 물고 오던 문제

한컴은 제목 표를 **`secPr`(페이지 설정)과 같은 문단 안에** 넣기도 합니다.
`analyze` 가 그 문단을 통째로 페이지 설정 템플릿으로 잡으면서, 조판하는 모든
문서에 **원본 문서의 제목이 그대로 딸려 들어갔습니다.**

→ 페이지 설정만 남기고 표·그림은 떼도록 했습니다.

## 2. 제목행 칸이 내용보다 낮게 잡히던 문제

표 행 높이를 계산할 때 **모든 행을 본문 글꼴 기준**으로 쟀습니다. 일차 제목행처럼
본문보다 큰 글꼴을 쓰는 행은 줄 수가 모자라게 계산돼 칸이 낮아졌고, 품질 검문이
`칸이 낮다(11mm, 2줄엔 16mm 필요)` 로 잡아냈습니다.

→ 칸마다 **그 칸의 글꼴 크기**로 줄 수를 세도록 했습니다.

## 3. 문서

`.hwp` 레퍼런스는 `analyze` 가 읽지 못합니다. 워크플로우 R 에 변환 단계를
명시했습니다.

```bash
python3 scripts/convert_hwp.py <ref.hwp> -o <ref.hwpx>   # ← 추가
python3 scripts/doc_spec.py analyze <ref.hwpx> -o spec/
```

## 검증

`tests/test_doc_spec.py` 48케이스 — `first_para` 가 페이지 설정은 보존하되
표·그림은 안 물고 오는지, 큰 글꼴 제목행이 낮게 잡히지 않는지 확인합니다.
전체 22개 테스트 파일 통과.
