# v1.12.2 — 표 셀 글자가 겹쳐 찍히던 문제 수정 (2026-08-21)

`doc_spec.py render` 로 만든 표에서 **긴 셀 내용의 글자가 서로 겹쳐 찍히는**
문제가 있었습니다. 자간을 조정해도 해결되지 않는 종류의 증상입니다.

## 원인

레퍼런스의 표 셀 상당수가 `lineWrap="SQUEEZE"` 입니다. 긴 글을 줄바꿈하지 않고
**자간을 줄여 한 줄에 욱여넣는** 설정입니다. 원본 일정표는 셀 내용이 짧아
티가 나지 않았지만, 이 설정을 그대로 복제한 상태에서 내용이 길어지면 글자가
겹쳐 찍힙니다.

## 수정

조판 결과의 셀 줄바꿈을 **`BREAK`(줄바꿈)** 으로 강제합니다. 짧은 글에서는
BREAK 와 SQUEEZE 의 결과가 같으므로 기존 문서의 모양이 달라지지 않고, 긴 글은
정상적으로 여러 줄로 접힙니다.

## 검증

`tests/test_doc_spec.py` 에 4케이스를 더했습니다(총 27케이스) — 긴 셀 텍스트를
조판했을 때 `SQUEEZE` 가 남지 않는지 확인합니다. 전체 22개 테스트 파일 통과.
