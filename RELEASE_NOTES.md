# v1.12.4 — 조판 결과를 바이트 단위로 재현 가능하게 (2026-08-21)

같은 레퍼런스와 같은 원고를 넣으면 **항상 같은 파일**이 나오도록 고정했습니다.

## 문제

이미지를 넣은 문서에서, 같은 입력인데도 실행할 때마다 파일 바이트가 달랐습니다.
새로 추가하는 `BinData` 항목에 **현재 시각**이 압축 시각으로 박혔기 때문입니다
(나머지 엔트리는 원본에서 물려받아 `1980-01-01` 로 고정돼 있었습니다).

같은 초 안에 두 번 실행하면 우연히 같은 결과가 나와, 확인이 쉽게 통과해 버리는
종류의 문제였습니다. 실제로는 초가 넘어가는 순간 달라집니다.

## 수정

새로 넣는 `BinData` 항목의 압축 시각을 나머지 엔트리와 같은
`1980-01-01` 로 고정합니다.

## 확인된 재현 범위

| 조건 | 결과 |
|---|---|
| `analyze` 2회 | spec.json·템플릿 전부 동일 |
| `render` 3회 (이미지 없음) | 산출물 바이트 동일 |
| `render` 2회, 5초 간격 (이미지 포함) | 산출물 바이트 동일 |
| `analyze`+`render` 전체를 처음부터 2회 | 산출물 바이트 동일 |
| 원고를 다른 경로에 두고 실행 | 산출물 바이트 동일 |

## 검증

`tests/test_doc_spec.py` 에 2케이스를 더했습니다(총 34케이스) — 1.2초 간격으로
두 번 조판해 바이트가 같은지, 모든 엔트리의 압축 시각이 고정돼 있는지
확인합니다. 전체 22개 테스트 파일 통과.
