# v1.14.2 — 배너 템플릿에 페이지 설정이 딸려오던 문제 (2026-08-21)

한컴은 제목 배너를 **`secPr`(페이지 설정)이 든 첫 문단 안에** 넣기도 합니다.
`analyze` 가 그 블록을 배너 템플릿으로 통째로 뽑으면서, 조판 결과에 페이지
설정이 **두 번** 들어갔습니다.

## 증상

- 문서 앞에 **빈 쪽**이 하나 생김
- 또는 **제목이 아예 보이지 않음**(XML 에는 있는데 렌더링되지 않음)

두 증상 모두 파일 구조만 봐서는 원인을 찾기 어려웠습니다. `validate` 와
`check --strict` 는 통과했고, 자체 레이아웃 엔진도 1쪽에 내용이 있다고
보고했습니다. 실제 한컴 화면을 보고서야 드러났습니다.

## 수정

- 배너·박스·표·그림 템플릿을 추출할 때 **페이지 설정을 떼어냅니다.**
- 원본이 페이지 설정과 제목 배너를 한 문단에 담고 있었다면, 조판할 때도
  **같은 구조로 되돌립니다.** 단, 배너는 반드시 **별도 run** 으로 붙입니다 —
  `secPr` 이 든 run 안에 넣으면 한컴이 표를 그리지 않습니다.

## 검증

`tests/test_doc_spec.py` — 모든 템플릿에 페이지 설정이 없는지, 조판 결과의
페이지 설정이 정확히 1개인지 확인합니다. 전체 22개 테스트 파일 통과.
