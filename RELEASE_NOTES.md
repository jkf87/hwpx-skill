# v1.12.0 — 문서 재현: 레퍼런스 분석 → 새 내용 조판 (2026-08-21)

레퍼런스 문서 한 편을 주면 그 **편집 규범**을 뽑아, 새 내용을 같은 규범으로
조판하는 `scripts/doc_spec.py` 를 추가했습니다. 기존 "복제 후 치환" 이
구조적으로 만들어내던 레이아웃 깨짐을 근본에서 없앱니다.

## 왜 치환으로는 안 되는가 (실측)

`assets/gyehoek-reference.hwpx` 를 전수 조사한 결과:

- **글자 서식은 체계가 있다** — charPr 301개가 의미 속성(글꼴·크기·색·굵기)
  기준으로는 61종으로 접힌다.
- **문단 서식은 체계가 없다** — paraPr 242개가 의미 속성(정렬·들여쓰기·여백·
  줄간격) 기준으로도 **206종**. 사실상 문단마다 다르다. 각 줄의 들여쓰기가
  그 줄의 텍스트 길이에 맞춰 손으로 조정돼 있기 때문이다.

그래서 복제 후 텍스트만 바꾸면 모든 줄이 **옛 텍스트에 맞춰진 기하학**을 그대로
물고 있게 된다. 빈 박스(실측 사례: 95자 내용에 221mm 높이가 저장돼 있던 셀),
페이지 쪼개짐, 표 칸 밀림이 여기서 나온다.

## 어떻게 푸는가

| 무엇 | 처리 |
|---|---|
| header.xml (글꼴·스타일·테두리 정의) | 원본 것을 **그대로** 사용 → 충실도·유효성 보장 |
| section0.xml (본문 구조) | 내용에 맞춰 **새로 조판** → 필요한 만큼 문단·행 생성 |
| 줄배치 캐시 `linesegarray` | **만들지 않음** → 한컴이 열 때 재계산 |

내용이 길든 짧든 레이아웃이 어긋날 수 없습니다.

## 사용법

```bash
python3 scripts/doc_spec.py analyze <ref.hwpx> -o spec/
python3 scripts/doc_spec.py render  spec/ <content.md> -o out.hwpx
```

`analyze` 가 문서를 역할별로 분해합니다 — 본문 계층별 대표 서식(□/ㅇ/-/*/※/
￭/⇒), 제목 배너(표지·장·절), 강조 박스, 제목 있는 박스, 데이터 표, 이미지
배치. gyehoek-reference 기준으로 본문 계층 7종, 배너 3종, 박스 2종, 표 1종,
이미지 배치가 추출됩니다.

## 원고 문법

```markdown
# 문서 제목                  → 표지 배너
## Ⅰ. 장 제목                → 장 배너
### 1 절 제목                → 절 배너([번호][제목])
□ 대항목 / ㅇ 중항목 / - 소항목 / * 각주 / ※ 참고 / ￭ 실적
⇒ 결론 문장                  → 강조 박스
::: <박스 제목>  …  :::      → 제목 있는 박스
| 분야 | 과제명 | 일정 |     → 데이터 표(내용 행 수만큼 자동 생성)
![](사진.png)                → 이미지(원본 비율 유지, 본문 폭에 맞춤)
```

## 구현에서 짚은 것

- **표는 병합 없는 셀 하나를 원자 단위로 삼아 다시 조립**합니다. 원본 일정표는
  '분야' 라벨 칸이 세로 병합돼 있어 그 행을 복제하면 칸이 모자라 내용이
  밀립니다(개발 중 실측·수정).
- 제목 박스와 데이터 표는 **열 수가 아니라 행 수로 역할을 구분**합니다. 열
  수만으로 묶으면 3열짜리 제목 박스와 3열짜리 일정표가 한 종류로 뭉쳐, 표가
  박스 서식으로 조판되는 사고가 납니다(개발 중 실측·수정).
- 이미지는 PNG/JPEG 픽셀 크기를 표준 라이브러리로 읽어 **원본 비율을 지키며**
  본문 폭에 맞춥니다. BinData 적재와 `content.hpf` 등록까지 처리합니다.

## 검증

- `tests/test_doc_spec.py` 18케이스 추가 — 규범 추출, 표가 내용 크기대로
  생성되는지, 줄배치 캐시가 0개인지, 최소·초장문 양극단, 원문 잔재 없음까지.
- 전체 22개 테스트 파일 통과.
