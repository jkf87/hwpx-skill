# v1.13.0 — 조판 품질 검문 + 행 높이 자동 계산 (2026-08-21)

**결정론은 품질을 보장하지 못합니다.** 지금까지 실제로 난 조판 사고 셋
(표 글자 겹침 · 배너 제목 3줄 접힘 · 빈 박스)은 전부 결정론적이었습니다.
같은 입력에 매번 똑같이 깨졌을 뿐입니다. 그래서 레이아웃 자체를 검사하고,
깨질 수 있는 자리는 조판 단계에서 스스로 맞추게 했습니다.

## 추가 — 조판 품질 검문

```bash
python3 scripts/doc_spec.py lint <out.hwpx>
```

`render` 가 끝나면 자동으로 실행되며, 오류가 있으면 exit 2 로 멈춥니다.

- **오류**: 줄배치 캐시 잔존 / `lineWrap="SQUEEZE"`(자간을 줄여 글자가 겹침) /
  표 칸 수가 행×열과 불일치(내용 밀림)
- **경고**: 칸이 내용보다 낮음(잘림) / 지나치게 높음(빈 박스) / 칸·그림이
  본문 폭 초과

## 개선 — 표 행 높이를 내용에 맞춰 계산

원본 행 높이를 그대로 복제하던 것을, **칸 폭과 글자 크기로 줄 수를 어림해**
높이를 계산하도록 바꿨습니다.

- 글 폭은 **실제 글자 크기**로 잽니다. 배너(큰 글꼴)와 표 본문(작은 글꼴)에
  같은 상수를 쓰면 표 높이가 몇 배로 부풀려집니다.
- 여백은 한 번만 더하고 추가 줄은 줄간격만 더합니다. 한 줄 높이를 줄 수만큼
  곱하면 4줄짜리 칸이 45mm 로 부풉니다(실제 필요한 건 31mm).

## 검증

`tests/test_doc_spec.py` 42케이스 — 정상 산출물은 검문 통과, 결함 있는 문서는
원인(SQUEEZE·줄배치 캐시)을 지목하며 exit 2, 행 높이가 내용에 따라 달라지는지
확인합니다. 전체 22개 테스트 파일 통과.
