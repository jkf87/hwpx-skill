# v1.15.0 — 겉공문(시행문) 서식을 실측값으로 맞춤 (2026-08-21)

실제 시행문(한국지능정보사회진흥원, 2026. 8. 21. 발신)의 PDF 글리프 좌표를
재서 기안문 생성기와 대조했습니다. 여섯 군데가 어긋나 있었습니다.

## 서식 (`scripts/gonmun.py`)

| 항목 | 기존 | 실측 | 조치 |
|---|---|---|---|
| 원훈(표어) | 없음 | 상단 중앙 9pt | 추가 (`"원훈"`) |
| 두문 구분선 | 없음 | 제목 아래 검정 0.4mm 실선 | 추가 |
| 결문 구분선 | `─` 46개 텍스트 | 회색 `#CCCCCC` 3.0mm | 문단 아래 테두리로 교체 |
| 결재란 | 한 줄 텍스트 | 직위칸 52pt + 서명칸 65pt, 서명칸 위 결재일자 8pt | 테두리 없는 표 |
| 발신명의 | 22pt | 15pt | 실측값 |
| 결문 라벨 | 전화 / 전송 | 전화번호 / 팩스번호 | 시행규칙 별지 제1호서식 표기 |

**결재란을 표로 짠 이유** — 직위가 「연구원」이냐 「정보화과장」이냐에 따라
탭으로는 성명 x좌표가 밀립니다. 실제 문서는 칸마다 좌표가 정확히 같습니다.

**구분선을 문단 테두리로 바꾼 이유** — `'─'` 반복은 글꼴마다 길이가 달라지고
본문 폭에 맞지 않습니다. 문단 아래 테두리는 본문 폭 전체에 정확히 그려집니다.

## 줄간격

편람이 적어 둔 **103%로 짜면 본문이 눌려 붙어 공문으로 보이지 않습니다.**
실측 시행문은 12pt 글자에 줄 간격 19pt — 약 160%입니다. 기본값을 160%로
바꿨습니다. 최상위 항목(`1.` `2.` `3.`) 사이 빈 줄도 자동으로 넣습니다.

```json
"줄간격": 130,          // 기본 160
"항목간_빈줄": false    // 기본 true
```

`줄간격`을 지정하면 `header.xml` **사본**을 만들어 씁니다(원본 템플릿 불변).

## 검수기 (`scripts/gonmun_lint.py`)

- `DATE_KOREAN` 추가 — `2026년 8월 25일` → `2026. 8. 25.`
- `COLON_SPACE` 오탐 수정 — `http://` 의 `://` 를 쌍점 규칙 위반으로 잡고
  있었습니다. 결문에 홈페이지가 있는 **모든** 공문에서 났습니다.

## 호환

예전 입력(`기안자`/`검토자`/`결재권자`, `전화`/`전송`)은 그대로 받아 자동
변환합니다. 기존 JSON을 고치지 않아도 됩니다.

## 스타일 (`templates/gonmun2025/header.xml`)

borderFill 5·6(아래 테두리만), charPr 16·17·18(15pt 굵게 / 9pt 굵게 / 8pt),
paraPr 26·27·28(구분선 3종) 추가. 결문선을 편람 표준 검정 실선으로 바꾸려면
`"결문선": "검정"`.

## 검증

기안문 테스트 26 → **56건**. 새 스타일 ID가 `header.xml` 에 실제로 정의돼
있는지도 검사합니다(없으면 한컴이 파일을 못 엽니다). 전체 24개 테스트 파일 통과.
