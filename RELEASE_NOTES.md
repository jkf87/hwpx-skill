# v1.13.1 — 기관마다 다른 계층 기호를 함께 인식 (2026-08-21)

`ㅇ`(한글 이응, U+3147)과 `○`(흰 원, U+25CB)는 화면에서 구별되지 않지만
**다른 문자**입니다. 지금까지는 `ㅇ` 만 알고 있어, `○` 를 쓰는 문서를 분석하면
그 줄들이 전부 계층 없는 일반 문단으로 뭉개졌습니다.

실제로 다른 기관 문서(`○` 사용)를 분석하려다 발견했습니다.

## 수정

같은 층위에 쓰이는 표기를 **별칭으로 묶어** 인식합니다.

| 층위 | 인식하는 기호 |
|---|---|
| 대항목 | `□` `▪` `■` |
| 중항목 | `ㅇ` `○` `◦` `●` |
| 결론 | `⇒` `→` `➡` |
| 실적 | `￭` `▣` |
| 세부 | `▸` `▶` `►` |
| 참고 | `※` |
| 소항목 | `-` `–` `‐` |
| 각주 | `*` `＊` |

## 검증

`tests/test_doc_spec.py` 에 2케이스를 더했습니다(총 44케이스) — `○` 와 `ㅇ` 가
같은 층위로 분류되는지, 다른 기관 표기(`▶`, `＊`)도 인식되는지 확인합니다.
전체 22개 테스트 파일 통과.
