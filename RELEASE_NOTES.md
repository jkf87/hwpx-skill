# v1.12.1 — 마크다운 굵게(`**`)가 별표로 새던 문제 수정 (2026-08-21)

v1.12.0 의 `doc_spec.py render` 가 `**굵게**` 표기를 그대로 흘려보내, 문서에
별표가 글자로 박히는 문제가 있었습니다. 외부 에이전트에게 실제로 원고를
맡겨 본 결과 표 셀에서만 **10곳**이 발생했습니다 — 사람이 마크다운을 쓰면
자연스럽게 나오는 표기라 재발 가능성이 높은 자리였습니다.

한글은 마크다운 렌더러가 아니므로 `**` 는 반드시 조판 단계에서 처리해야
합니다.

## 수정

- `**굵게**` 를 **실제 굵게 런으로 분리**해 조판합니다.
- 굵게 charPr 는 원본 header 에서 단계적으로 찾습니다:
  같은 (글꼴·크기·색) → 같은 (글꼴·크기) → 같은 글꼴.
  정확히 일치하는 짝은 드물기 때문입니다(실측: 301개 charPr 중 굵게 100개,
  세 속성이 모두 맞는 짝은 11종뿐).
- 끝내 굵게 짝을 못 찾으면 **별표만 제거**합니다. 어떤 경우에도 별표가 글자로
  남지 않습니다.
- 본문 문단·표 셀·박스·배너 전 경로에 적용됩니다.

## 검증

`tests/test_doc_spec.py` 에 5케이스를 더했습니다(총 23케이스) — 별표 잔존
0건, 강조 텍스트 보존, **굵게 charPr 이 실제로 적용됐는지**(별표만 지운 게
아닌지)까지 확인합니다. 전체 22개 테스트 파일 통과.
