# v1.14.0 — 표지·목차·들여쓰기 계층 (2026-08-21)

보고서·계획서처럼 분량이 있는 문서에 필요한 구조를 `doc_spec` 에 더했습니다.
공문서 생성 도구 [kordoc](https://github.com/chrisryugj/kordoc) 의 편집 관행을
참고했고, 구현은 이 저장소에서 독자적으로 했습니다(출처·라이선스는
`THIRD_PARTY_NOTICES.md`).

## 추가

```bash
python3 scripts/doc_spec.py render spec/ 원고.md -o out.hwpx \
    --cover-page --toc --org "기관명" --date "2026. 8. 21."
```

- **`--cover-page`** — 제목을 **독립 표지 쪽**으로 세우고 아래에 날짜·기관을 넣는다.
- **`--toc`** — 장(`##`) 제목을 모아 **목차 쪽**을 만든다.
- **들여쓰기 계층** — 원고에 기호를 직접 쓰지 않고 목록 들여쓰기만으로 층위를
  표현할 수 있다. 레퍼런스에서 뽑은 기호가 자동으로 붙는다.

```markdown
- 최상위 항목            → ㅇ 최상위 항목   (0칸 = 중항목)
  - 한 단계 아래         →    - 한 단계 아래 (2칸 = 소항목)
```

기호는 레퍼런스에서 추출한 것을 쓰므로, `ㅇ` 을 쓰는 문서와 `○` 를 쓰는 문서가
각각 자기 기호로 조판됩니다.

## 장 시작 쪽 나눔

장 배너에 쪽 나눔이 걸린 레퍼런스를 쓰면, 조판 결과도 **장마다 새 쪽에서
시작**합니다. 원본 서식을 그대로 쓰는 이 도구의 원칙이 그대로 적용된 결과입니다.

## 검증

`tests/test_doc_spec.py` 56케이스 — 목차에 장 제목이 모이는지, 표지에 기관·날짜가
들어가는지, 목록 들여쓰기가 레퍼런스의 계층 기호로 바뀌는지, 결과가 배포 게이트
(`check --strict`)를 통과하는지 확인합니다. 전체 22개 테스트 파일 통과.
