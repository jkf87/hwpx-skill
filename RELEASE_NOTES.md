# v1.16.0 — 지자체 실무부서 보고서 서식 2종: 요약보고·기본계획 (2026-08-21)

한 지자체 실무부서의 실제 산출물 15건을 분류해, 가장 많이 쓰는 두 서식을
**마크다운 → HWPX 생성기**로 익혔습니다. 문서를 복제한 게 아닙니다 — XML 을 재서
값마다 "누가 정했나"를 갈라 적고, 기관 층은 상수로, 내용 층은 계산으로, 글쓴이
층은 마크다운으로 옮겼습니다. **원본 문서의 기관명·인명·로고·전용 글꼴·그림
브러시는 하나도 남기지 않았고**, 테스트가 grep 으로 확인합니다.

## 요약보고 — `scripts/yoyak.py` (워크플로우 Y)

단장·팀장 결재를 받아 올리는 1~3쪽 약식 보고. 왼쪽 위 로고(또는 기관명), 오른쪽 위
**결재선 표**(보고일 / 직위 | 성명, 점선 경계), 색 띠 사이 제목, `□ ❍ - ⇒ ※` 개조식,
머리행 음영 표.

```markdown
---
기관: ○○시 ○○추진단
보고일: 2026. 8. 21.(금)
결재: 추진단장 홍길동(1234) / 정책팀장 김철수(1235)
---
# 제목
## 소제목
- 항목
  - 세부
⇒ 결론
```

## 기본계획·검토보고 — `scripts/geomto.py` (워크플로우 Y)

`▐Ⅰ▌ 추진배경 → ▐Ⅱ▌ 현황 → ▐Ⅲ▌ 세부계획 …` 로마숫자 장 배너로 나뉘는 다쪽
계획서. 연파랑·남색 띠 사이 제목, 리드 요약 박스, `❍ - ▸ ⇒ ※ *` 개조식.
`표지: true` 면 문서정보·결재란·제목 띠·기관명이 든 **표지** 한 장이 앞에 붙습니다.

```markdown
---
부제: - ○○을 위한 -
표지: true
결재: 주무관 / 팀장 / 추진단장 / 부시장 / 시장
---
# 제목
> 리드 요약
## 추진배경
- 항목
```

## 공통

- **결정론** — 같은 마크다운이면 같은 바이트(zip 엔트리 전부 1980-01-01, 날짜 메타는 보고일).
- **계산하는 것** — 결재선 칸 높이(인원수), 제목·배너 폭(용지), 표 칸 너비(글자 수),
  표지 결재 칸 수(인원수), 박스 높이(줄 수). 박아 둔 좌표가 없습니다.
- **형식의 세 층** — `references/yoyak-bogo-style.md`, `references/geomto-bogo-style.md`
  에 실측값과 어느 층에 속하는지를 적었습니다. 다른 부서 서식으로 바꿀 때 기관 층만
  갈아 끼우면 됩니다.
- 헤더는 `geomto` 가 `yoyak` 의 상위 집합이라 본문 규칙을 공유합니다.

## 검증

`tests/test_yoyak.py` 44건, `tests/test_geomto.py` 35건. 전체 26개 테스트 파일 통과.
